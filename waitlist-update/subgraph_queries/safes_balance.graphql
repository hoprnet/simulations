query ($first: Int!, $skip: Int!) { 
    safes(first: $first, skip: $skip) {
        id
        balance {
          wxHoprBalance
        }
        registeredNodesInNetworkRegistry { 
            node { 
                id 
            } 
        }
    }
}